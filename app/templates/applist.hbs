{{!-- TBD: dynamic instead of hardcode --}}


  {{#each view.controller.screens}}
  {{#if hasApp}}
  {{view 'appscreen' index=id hasApp=hasApp}}
  {{/if}}
  {{/each}}

  {{!-- <ul> --}}
    {{#each app in view.controller.model}}
    {{view 'appicon' content=app}}
    {{/each}}
  {{!-- </ul> --}}
  <div class='hint'></div>
